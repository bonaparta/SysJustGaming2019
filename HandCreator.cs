class HandCreator
{
    Tuple<Hand, Queue<Card>> Arrange(Queue<Card> cards)
    {
        Tuple<bool, Hand> pairHand = IsStraightFlush
    }

    private static Tuple<bool, Hand?> IsStraightFlush(Queue<Card> cards) {
        return true;
    }

    private static Tuple<bool, Hand?> IsStaightFlush(Queue<Card> cards) {
        return true;
    }

    private static Tuple<bool, Hand?> IsFourOfAKind(Queue<Card> cards) {
        return true;
    }

    private static Tuple<bool, Hand?> IsFullHouse(Queue<Card> cards) {
        return true;
    }

    private static Tuple<bool, Hand?> IsFlush(Queue<Card> cards) {
        return true;
    }

    private static Tuple<bool, Hand?> IsThreeOfAKind(Queue<Card> cards) {
        return true;
    }

    private static Tuple<bool, Hand?> IsTwoPair(Queue<Card> cards) {
        return true;
    }

    private static Tuple<bool, Hand?> IsOnePair(Queue<Card> cards) {
        return true;
    }
}
